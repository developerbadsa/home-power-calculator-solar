/**
 * Example solar panel sizes (W) commonly available in the market.
 * Used only to generate example combinations (§23); the generic
 * recommendation is the total wattage, independent of any inventory (§47).
 */
export const SOLAR_PANEL_WATTS = [100, 150, 200, 300, 330, 400, 450, 550] as const;